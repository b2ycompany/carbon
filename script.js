document.getElementById('carbonForm').addEventListener('submit', function(event) { event.preventDefault(); // Coeficiente de emissão de CO2 para carros a combustão (em kg CO2 por km) const co2PorKm = 0.12; // Exemplo: 120g de CO2 por km // Valor do crédito de carbono por kg de CO2 sequestrado (em R$) const valorCreditoPorKg = 0.10; // Exemplo: R$ 0,10 por kg de CO2 // Captura os quilômetros rodados const kmRodados = parseFloat(document.getElementById('kmRodados').value); // Calcula o CO2 sequestrado const co2Sequestrado = kmRodados * co2PorKm; // Calcula o valor do crédito de carbono const creditoCarbono = co2Sequestrado * valorCreditoPorKg; // Exibe o resultado const resultadoDiv = document.getElementById('resultado'); resultadoDiv.innerHTML = ` <p>CO2 Sequestrado: ${co2Sequestrado.toFixed(2)} kg</p> <p>Crédito de Carbono: R$ ${creditoCarbono.toFixed(2)}</p> `; });
