document.addEventListener("DOMContentLoaded", () => { const introContainer = document.querySelector(".intro-container"); const mainContainer = document.querySelector(".main-container"); // Garante que a transição funcione setTimeout(() => { introContainer.style.display = "none"; // Esconde a tela de introdução mainContainer.classList.remove("hidden"); // Exibe o site principal }, 3000); // 3 segundos });
